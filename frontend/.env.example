# API Base URL
# iOS 시뮬레이터는 localhost 사용 가능
# 실제 디바이스는 Mac의 로컬 IP 주소 필요
API_BASE_URL=http://localhost:8000/api/v1

# Device API Base URL (실제 디바이스용)
# Mac의 로컬 IP를 확인 후 수정하세요: ifconfig | grep "inet " | grep -v 127.0.0.1
DEVICE_API_BASE_URL=http://192.168.75.69:8000/api/v1
